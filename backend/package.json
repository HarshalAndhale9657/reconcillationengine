{
  "name": "reconciliation-engine-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Backend monorepo for Reconciliation Engine",
  "scripts": {
    "build": "pnpm --filter './packages/*' --filter './services/*' build",
    "dev:ingestion": "pnpm --filter ingestion-service dev",
    "dev:reconciliation": "pnpm --filter reconciliation-service dev"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}

