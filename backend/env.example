# Docker Compose Configuration
# Note: Each service uses its own .env file in its directory
# See services/ingestion-service/.env.example
# See services/reconciliation-service/.env.example

# Kafka Port (for external access)
KAFKA_PORT=9092
